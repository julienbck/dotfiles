[color]

  diff = true
  ui = true

[color "diff"]

  whitespace = red

[core]

  excludesfile = ~/.gitignore
  pager = less -F -X

[fetch]

  prune = true

[init]

  templatedir = ~/.git_template

[grep]

  extendRegexp = true
  lineNumber = true

[push]

  default = current

[user]

  useconfigonly = true
	email = julien.bock@decathlon.com
	name = JULIEN BOCK
[filter "lfs"]
	clean = git-lfs clean -- %f
	smudge = git-lfs smudge -- %f
	process = git-lfs filter-process
	required = true
